meta {
  name: Borrow Book
  type: http
  seq: 12
}

post {
  url: http://localhost:8019/library/borrow-book
  body: json
  auth: none
}

body:json {
  {
    "isbn": "978-0141318202"
  }
}

assert {
  res.status: eq 200
  res.body: eq Book borrowed successfully
}
